107|Part 2 answers (Spy database). I ran each query on my local Spy database from Activity 04-2.
108|
109|List the name of the team with the highest team_id.
110|
111|Answer 6:
112|SQL (subquery):
113|SELECT name
114|FROM team
115|WHERE team_id = (
116|  SELECT MAX(team_id)
117|  FROM team
118|);
119|
120|Rows returned: [paste count after you run]
121|[SCREENSHOT: first 5 rows of query result]
122|
123|List the first and last names of agents who have been on a Top Secret mission that failed.
124|
125|Answer 7:
126|SQL (subquery): Agents on a mission are on that mission's team (teamrel links agent to team; mission has team_id).
127|SELECT DISTINCT a.first, a.last
128|FROM agent a
129|WHERE a.agent_id IN (
130|  SELECT tr.agent_id
131|  FROM teamrel tr
132|  WHERE tr.team_id IN (
133|    SELECT m.team_id
134|    FROM mission m
135|    WHERE m.access_id IN (
136|      SELECT sc_id
137|      FROM securityclearance
138|      WHERE sc_level = 'Top Secret'
139|    )
140|    AND lower(m.mission_status) = 'failed'
141|  )
142|);
143|
144|Rows returned: [paste count after you run]
145|[SCREENSHOT: first 5 rows — or all rows if fewer than 5]
146|
147|Note: If your data uses a different status spelling, adjust the failed filter (for example 'Failed').
148|
149|Find the language and number of speakers for the language or languages with the most speakers.
150|
151|Answer 8:
152|SQL (subquery):
153|SELECT l.language, COUNT(*) AS number_of_speakers
154|FROM language l
155|INNER JOIN languagerel lr ON l.lang_id = lr.lang_id
156|GROUP BY l.lang_id, l.language
157|HAVING COUNT(*) = (
158|  SELECT MAX(speaker_count)
159|  FROM (
160|    SELECT COUNT(*) AS speaker_count
161|    FROM languagerel
162|    GROUP BY lang_id
163|  ) AS counts_per_language
164|);
165|
166|Rows returned: [paste count after you run]
167|[SCREENSHOT: first 5 rows — or all rows if fewer than 5]
168|
169|Explanation: Count agents per language, then keep only language(s) whose count equals the maximum.
170|
171|Part 3: CREATE TABLE Using a Subquery — 15 points (5*3 each)
172|Run the following SQL statement in the Spy database that you loaded during one of the class activities. If you try running this on the pre-loaded shared version of the Spy database, you may receive a permission error because you do not have table creation privileges there.
173|If you do not already have your own local copy of the Spy database, I strongly recommend loading it from the spy.sql file provided in  Activity 04-2 before completing this question.
174|
175|CREATE TABLE newagent AS
176|SELECT *
177|FROM agent
178|WHERE TRUE = FALSE;
179|Answer the following:
180|9a. Explain what this statement creates.
181|
182|Answer 9a: