109 'List the name of the team with the highest team_id.'
111 'Answer 6:'
112 'SQL (subquery) ù table team (team_id, name) from ERD:'
113 'SELECT name'
114 'FROM team'
115 'WHERE team_id = ('
116 '  SELECT MAX(team_id)'
117 '  FROM team'
118 ');'
120 'Rows returned: [paste count after you run]'
121 '[SCREENSHOT: first 5 rows of query result]'
123 'Answer 7:'
124 'SQL (subquery) ù path from ERD: agent -> teamrel -> team <- mission; mission.access_id -> securityclearance:'
125 'SELECT DISTINCT a.first, a.last'
126 'FROM agent AS a'
127 'WHERE a.agent_id IN ('
128 '  SELECT tr.agent_id'
129 '  FROM teamrel AS tr'
130 '  WHERE tr.team_id IN ('
131 '    SELECT m.team_id'
132 '    FROM mission AS m'
133 '    INNER JOIN securityclearance AS sc'
134 '      ON m.access_id = sc.sc_id'
135 "    WHERE sc.sc_level = 'Top Secret'"
136 "      AND lower(m.mission_status) = 'failed'"
137 '  )'
138 ');'
140 'Rows returned: [paste count after you run]'
141 '[SCREENSHOT: first 5 rows ù or all rows if fewer than 5]'
143 'Answer 8:'
144 'SQL (subquery) ù tables language and languagerel (lang_id, agent_id) from ERD:'
145 'SELECT l.language, COUNT(*) AS number_of_speakers'
146 'FROM language AS l'
147 'INNER JOIN languagerel AS lr ON l.lang_id = lr.lang_id'
148 'GROUP BY l.lang_id, l.language'
149 'HAVING COUNT(*) = ('
150 '  SELECT MAX(speaker_count)'
151 '  FROM ('
152 '    SELECT COUNT(*) AS speaker_count'
153 '    FROM languagerel'
154 '    GROUP BY lang_id'
155 '  ) AS counts_per_language'
156 ');'
158 'Rows returned: [paste count after you run]'
159 '[SCREENSHOT: first 5 rows ù or all rows if fewer than 5]'
161 'Explanation: Each row in languagerel links one agent_id to one lang_id; COUNT is number of agents per language.'
171 'Part 3: CREATE TABLE Using a Subquery ù 15 points (5*3 each)'
172 'Run the following SQL statement in the Spy database that you loaded during one of the class activities. If you try running this on the pre-loaded shared version of the Spy database, you may receive a permission error because you do not have table creation privileges there.'
173 'If you do not already have your own local copy of the Spy database, I strongly recommend loading it from the spy.sql file provided in  Activity 04-2 before completing this question.'
175 '\uec03CREATE TABLE newagent AS'
176 'SELECT *'
177 'FROM agent'
178 'WHERE TRUE = FALSE;'
179 '\uec02Answer the following:'
180 '9a. Explain what this statement creates.'
182 'Answer 9a:'
183 'The statement creates a new table named newagent with the same column names and data types as agent, but with zero rows. From the Spy ERD, agent has: agent_id, first, middle, last, address, city, country, salary, clearance_id ù newagent gets those nine columns with matching types and no data.'
185 '9b. Why is TRUE = FALSE used in the query?'
187 'Answer 9b:'
188 'WHERE TRUE = FALSE is always false, so no rows from agent are copied. I get the column structure only (CREATE TABLE ... AS SELECT with an empty result).'
190 '9c. Include a screenshot showing that newagent has the same columns as agent.'
192 'Answer 9c:'
193 '[SCREENSHOT: \\d agent and \\d newagent in psql, or DBeaver column list for both tables ù show the nine columns above match (agent_id, first, middle, last, address, city, country, salary, clearance_id)]'
195 '9d. Does newagent automatically copy the same primary key and foreign key constraints as agent? Explain.'
197 'Answer 9d:'
198 'No. CREATE TABLE ... AS SELECT copies columns and data types only; it does not copy constraints from agent. On the ERD, agent has a primary key on agent_id and a foreign key clearance_id -> securityclearance.sc_id ù those are not recreated on newagent unless I add them with ALTER TABLE.'
200 '9.e Drop the table when finished and include the SQL command you used.'
202 'Answer 9e:'
203 'DROP TABLE newagent;'
205 '[SCREENSHOT optional: message showing table dropped]'
207 '\uec03DROP TABLE newagent;'
208 '\uec02'
209 'Part 4: Transactions ù 30 points (6*5 each)'
